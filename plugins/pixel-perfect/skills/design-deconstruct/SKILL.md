---
name: design-deconstruct
description: Deconstruct UI code, URLs, screenshots, concepts, or HTML into a token-governed atomic design system and real target-framework components when configured.
---

# Pixel Perfect: Design Deconstruct

Read [the runtime contract](../../workflows/RUNTIME-CONTRACT.md). If `design/manifest.json` or `design/manifest.yaml` exists, read [the process context](../process-context/SKILL.md). Then read [the complete design-deconstruct workflow](../../workflows/design-deconstruct.md) and execute it as authoritative.

Load the bundled deconstruct-engine and preserve every preflight, render, token, coverage, and audit gate.

Invoke with the active harness's syntax from the harness mappings table in the runtime contract, treat the user's remaining text as input, collect choices through that harness's input mechanism, and represent transient workflow tasks with its planning tools. Durable completion comes only from the manifest and required evidence.
